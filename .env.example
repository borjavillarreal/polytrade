# Copy this file to ".env" and paste your real key after the = sign.
# Get a key at https://console.anthropic.com  ->  Settings -> API keys -> Create key.
# This is the ONLY credential the project needs. No Polymarket key/wallet is used.
# The real .env file is gitignored and never committed.

ANTHROPIC_API_KEY=sk-ant-paste-your-key-here
